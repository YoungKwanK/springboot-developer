spring:
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true # 쿼리 실행 구문 보여주는 옵션
    properties:
      hibernate:
        format_sql: true

    defer-datasource-initialization: true # 테이블 생성 후에 data.sql 실행
jwt:
  issuer: ajufresh@gmail.com
  secret_key: study-springboot